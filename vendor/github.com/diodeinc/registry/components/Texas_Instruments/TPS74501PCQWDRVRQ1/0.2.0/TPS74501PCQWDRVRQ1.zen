"""
TPS74501PCQWDRVRQ1

Auto-generated using `pcb search`.
"""

Pins = struct(
    EN=io("EN", Net),
    EP=io("EP", Net),
    FB=io("FB", Net),
    GND=io("GND", Net),
    IN=io("IN", Net),
    OUT=io("OUT", Net),
    PG=io("PG", Net),
)

Component(
    name="TPS74501PCQWDRVRQ1",
    symbol=Symbol(library="TPS74501PCQWDRVRQ1.kicad_sym"),
    footprint=File("TPS74501PCQWDRVRQ1.kicad_mod"),
    pins={"EN": Pins.EN, "EP": Pins.EP, "FB": Pins.FB, "GND": Pins.GND, "IN": Pins.IN, "OUT": Pins.OUT, "PG": Pins.PG},
)
