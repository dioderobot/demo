# DM0001 Bringup Session Log

Date: 2026-04-12
Workspace: `/Users/nasheed/GitHub/demo`

## Actions

1. Located the DM0001 board sources, DPS-150 control repo, and available host tools.
2. Confirmed `probe-rs 0.30.0` is installed and detects `Diode Probe v3` over CMSIS-DAP.
3. Identified serial devices:
   - `/dev/cu.usbmodem065AD9D205B31` -> DPS-150 (`AT32 Virtual Com Port`, vendor `Artery`)
   - `/dev/cu.usbmodem1102` -> `Diode Probe v3`
4. Queried the DPS-150 state in read-only mode. Result:
   - output `OFF`
   - setpoint `5.499 V`
   - current limit `3.000 A`
   - input supply `20.48 V`
5. Parsed the DM0001 board source and local MCU/gate-driver docs.
6. Found a board metadata discrepancy:
   - board `.zen` and PCB properties name `STM32G431C8T6`
   - board docs/BOM point to `STM32G431CBU6`
7. Inferred that the three current-sense channels are intended to use the STM32 internal OPAMPs in PGA mode, because the external resistor network alone would not provide practical ADC dynamic range.
8. User confirmed the assembled board population is `STM32G431C8`.
9. Created an Embassy bringup firmware crate at `boards/DM0001/bringup/firmware` and built a release image successfully for `thumbv7em-none-eabihf`.
10. Release image size check (`cargo size --release --bin dm0001-bringup -- -A`) reported:
    - `.text`: `28356`
    - `.rodata`: `5780`
    - `.data`: `152`
    - `.bss`: `216`
    - total image footprint: `36271` bytes
11. The bringup firmware currently:
    - configures the MCU for HSI `16 MHz`
    - forces all six TIM1 gate-input pins low as GPIO outputs
    - configures TIM1 without muxing PWM outputs to the bridge
    - enables ADC1/ADC2, OPAMP1/2/3, RTT logging, and structured telemetry output
12. Powered the board from the DPS-150 with guarded limits:
    - protection ceilings set to `OVP=12.0 V`, `OCP=2.0 A`
    - tested at `6.0 V / 0.5 A` and again at `8.0 V / 0.5 A`
13. Confirmed from `boards/DM0001/docs/DM0001.md` that DM0001 is designed for `8-28 V` input and uses the power tree:
    - `VPLUS -> 10V buck -> 5V LDO -> 3V3 LDO`
14. Observed live hardware behavior during guarded power-up:
    - board initially powers and draws about `35 mA`
    - motor spins immediately on startup
    - DPS-150 output then shuts off and reports protection `REP`
15. `probe-rs info --chip STM32G431C8 --protocol swd --connect-under-reset` succeeded during the brief powered window, confirming the MCU is reachable over SWD before the supply trips.
16. User reported the board is now mechanically/electrically stable enough to attempt flashing.
17. Verified the DPS-150 was on and stable before flash. Initial steady state observed:
    - output enabled
    - setpoint `5.0 V`
    - current limit `0.5 A`
    - measured load current about `33 mA`
    - protection `OK`
18. Verified plain SWD attach at the stable condition with:
    - `probe-rs info --chip STM32G431C8 --protocol swd --non-interactive`
19. Flash attempt with `--connect-under-reset` failed:
    - `Timeout while attaching to target under reset`
20. Flash attempt without `--connect-under-reset` succeeded:
    - `probe-rs download --chip STM32G431C8 --protocol swd --non-interactive --verify .../release/dm0001-bringup`
    - programming and verify completed successfully in `2.72 s`
21. Post-flash power/startup verification:
    - raised DPS setpoint to `8.0 V`
    - kept current limit at `0.5 A`
    - issued `probe-rs reset --chip STM32G431C8 --protocol swd --non-interactive`
    - sampled DPS state for ~3 s after reset
22. Post-reset DPS samples were all stable:
    - output remained `ON`
    - output voltage remained `8.0 V`
    - output current stayed around `10.5-10.7 mA`
    - protection remained `OK`
    - no motor spin was observed by the user
23. Tried to use `probe-rs run` and `probe-rs attach` for RTT/defmt log capture, but both currently fail with:
    - `Failed to parse defmt data`
    - `defmt version found, but no .defmt section - check your linker configuration`
24. Fixed the local bringup firmware linker configuration for defmt by adding `-Tdefmt.x` and rebuilding. Size check then showed a merged `.defmt` section:
    - `.text`: `28380`
    - `.rodata`: `5780`
    - `.defmt`: `69`
25. Inspected GitHub PR `#25` (`origin/feat/firmware`) because the user asked to use that as the only firmware reference. Relevant RTT details found there:
    - `build.rs` adds `--nmagic`, `-Tlink.x`, and `-Tdefmt.x`
    - `.cargo/config.toml` uses `probe-rs run --chip STM32G431C8 --log-format "{L} {s}"`
26. Checked out the PR in an isolated worktree at `/tmp/demo-pr25` and ran its safe `blinky` firmware:
    - command: `cargo run --bin blinky --release`
    - observed continuous RTT output such as:
      - `INFO  DM0001 Blinky Started!`
      - `INFO  Starting blink loop...`
      - repeated `INFO  Status LED ON/OFF`
27. Added a local safe RTT smoke-test binary at `boards/DM0001/bringup/firmware/src/bin/rtt_smoke.rs` that:
    - forces all six bridge pins low as GPIO outputs
    - blinks `PC15`
    - emits repeated defmt RTT logs without using async sleep
28. Built and ran the local smoke test successfully:
    - command: `DEFMT_LOG=trace cargo run --bin rtt_smoke --release`
    - observed continuous RTT output including:
      - `[INFO ] rtt_smoke_start`
      - `[INFO ] rtt_smoke_tick 0`
      - `[INFO ] rtt_smoke_tick 1`
      - through at least `rtt_smoke_tick 12`
29. Conclusion from the smoke tests:
    - RTT transport, probe, and host tooling are working on the DM0001 board
    - the remaining issue is specific to the original sensor bringup binary startup path, not RTT itself
30. Re-ran the original sensor bringup firmware with `DEFMT_LOG=trace cargo run --bin dm0001-bringup --release` and confirmed it now streams correctly over RTT. Observed boot markers through full telemetry, including:
    - `boot_entry`
    - `clocks_and_hal_ready`
    - `opamp*_calibrate_done`
    - repeated `telemetry={...}` frames
31. Verified that the original sensor bringup firmware reports stable live measurements around:
    - `vdda_mv`: `3287-3291`
    - `bus_mv`: `7957-7978`
    - `phase_mv`: near `3278-3285`
    - `ntc_mv`: about `960-967`
    - `mcu_temp_mc`: about `20991-21892`
    - hall inputs: `[true,true,true]`
32. Updated `.cargo/config.toml` so the default developer path works without extra host environment setup:
    - runner now uses `probe-rs run --chip STM32G431C8 --log-format "{t} {L} {s}"`
    - `[env] DEFMT_LOG = "info"`
33. Verified the default command now works directly:
    - `cargo run --bin dm0001-bringup --release`
    - output is clean, timestamped RTT logs without requiring a manual `DEFMT_LOG=...` override
34. Final non-invasive DPS verification after reflashing the original sensor bringup firmware:
    - output `ON`
    - output voltage `8.0 V`
    - output current about `13.7 mA`
    - current limit `0.5 A`
    - protection `OK`
35. Re-read the Zener current-sense network to calibrate the analog model from source, not guesswork:
    - shunt resistor: `3 mΩ`
    - bias network per phase: `22 kΩ` to `3V3`, `2.2 kΩ` to `GND`, `1.5 kΩ` from shunt node
    - STM32 OPAMP PGA gain in firmware: `16`
    - derived nominal opamp output:
      - zero-current output: `2.057 V`
      - sensitivity: `27.429 mV/A`
36. Found the firmware root cause for the absurd `-39 A` readings:
    - `adc_raw_to_uv()` used `u32` arithmetic and overflowed in release builds
37. Fixed the current path and telemetry model in firmware:
    - switched microvolt conversion to `u64` intermediate math
    - added per-board startup current-zero calibration over `64` samples
    - changed telemetry labels from misleading `phase_mv` to `bemf_mv`
    - added explicit state reporting: `booting -> calibrating -> disarmed`
    - added `arm_ready` gating based on calibration plausibility, bus voltage window, and idle current window
38. Verified calibrated live output on hardware after reflashing:
    - startup calibration reported `measured_zero_mv=[2034,2049,2039]`
    - repeated telemetry reported `state=disarmed`
    - repeated telemetry reported `arm_ready=true`
    - idle phase currents now sit around tens of milliamps, typically about `-110 mA` to `+55 mA`, rather than tens of amps
39. Final DPS verification after the calibrated firmware run:
    - output `ON`
    - output voltage `8.0 V`
    - output current about `14.0 mA`
    - current limit `0.5 A`
    - protection `OK`
40. Added a host-side real-time dashboard at `boards/DM0001/bringup/dashboard/rtt_dashboard.py` with:
    - RTT log ingestion from `probe-rs attach` or `probe-rs run`
    - parsing of `telemetry={...}` frames from the bringup firmware
    - curses TUI by default
    - `--plain` mode for smoke testing and log capture
41. Added dashboard docs at `boards/DM0001/bringup/dashboard/README.md`.
42. Verified the default RTT-only dashboard path end-to-end with:
    - `python3 boards/DM0001/bringup/dashboard/rtt_dashboard.py --plain --duration 3`
    - observed repeated parsed updates such as:
      - `state=disarmed`
      - `arm_ready=True`
      - `bus=7968`
      - calibrated `current_ma=[...]`
43. Made DPS polling opt-in via `--psu` instead of default-on:
    - keeps the default dashboard startup fast and reliable
    - leaves a path for future live supply power integration without blocking the RTT view
44. Tried a more aggressive sine profile at `12.0 V / 0.5 A` with:
    - `220 Hz` electrical target
    - `14%` amplitude target
    - `16_000` update ramp
    - result: the DPS tripped to `REP` about `1.3 s` into the run
    - RTT showed the bus collapsing to about `9.24 V` just before probe loss
45. Reworked `spin_sine_open_loop.rs` to favor sustainable speed over brute-force torque:
    - added bus-aware amplitude backoff
    - added a bus abort floor to disarm before the bench supply trips
    - changed profile to:
      - ramp to `190 Hz`
      - hold for `8_000` updates
      - `11%` max commanded amplitude
46. Ran the bus-aware profile at `12.0 V / 0.75 A`:
    - DPS stayed `OK`
    - firmware self-disarmed on `bus_abort` at about `13.2 kHz x100` (`131.97 Hz`) instead of tripping the bench
    - this confirmed the protection path worked, but the run still left speed on the table
47. Increased DPS current limit incrementally and held voltage at `12.0 V`:
    - verified `1.0 A` cleanly
    - then moved to `1.25 A`
    - kept the same firmware image initially so current headroom was the only changed variable
48. Ran the `190 Hz / 11% / 5 s` profile at `12.0 V / 1.25 A`:
    - reached the full `190 Hz` hold
    - completed the hold section
    - DPS remained `ON`, `CV`, protection `OK`
    - observed soft `bus_backoff` events without any abort or supply trip
49. Pushed frequency higher on the same `1.25 A` setup by changing only the target electrical speed:
    - new target: `220 Hz`
    - kept `11%` amplitude ceiling and the same droop-aware hold
    - run completed through the full `220 Hz` hold and returned to `disarmed`
    - DPS remained `12.0 V`, protection `OK`, final output current returned to idle after the run
50. Reworked the sine drive into a feedback-assisted, open-FOC-style controller in `spin_sine_open_loop.rs`:
    - kept the same three-phase sine PWM and bus protection envelope
    - added a lightweight sensorless observer using:
      - phase-voltage residuals after subtracting the commanded phase vector
      - Clarke projection for observer residual alpha/beta
      - rough Park `d/q` current telemetry from the three current shunts
      - a small phase-trim loop gated on residual magnitude and electrical speed
    - added observer telemetry fields:
      - `obs_locked`
      - `obs_err`
      - `obs_trim_q8`
      - `obs_res_mv`
      - `dq_ma`
51. First hardware validation of the observer:
    - at `12.0 V / 1.0 A`, the controller reported `obs_locked=true` starting around `80.5 Hz`
    - phase trim remained small but nonzero at speed, proving the observer was active
    - the run still self-disarmed on bus sag at high speed instead of tripping the bench
52. Initial `1.25 A` observer run showed a late-hold `bus_abort`, so the recovery path was tuned without changing the observer itself:
    - increased `BUS_FEEDBACK_RECOVERY_UPDATES` from `64` to `256`
    - this made bus-backoff recovery significantly less aggressive
53. Final observer validation at `12.0 V / 1.25 A`:
    - observer locked consistently through the full speed range
    - reached and held the `220 Hz` electrical target to the end of the profile
    - saw several soft `bus_backoff` events near the top end, but no final `bus_abort`
    - run returned cleanly to `disarmed`
    - DPS remained `OK`
54. Attempted the next feedback step by adding a q-axis current target and amplitude-trim loop on top of the observer:
    - added `iq_target_ma`, `iq_error_ma`, and trim telemetry
    - first active assist tuning enabled too early and could add `+2%` amplitude
    - live result at `12.0 V / 1.25 A`: observer locked, but the run aborted early on bus sag at about `15.26 kHz x100` (`152.62 Hz`)
55. Retuned the q-axis assist to be much more conservative:
    - moved the enable point later in the ramp
    - limited assist to `+1%`
    - slowed the trim update rate and required extra bus headroom
    - live result still regressed versus the earlier observer-only baseline: bus abort at about `18.86 kHz x100` (`188.62 Hz`)
56. Removed the q-axis trim path from the runtime image and restored `spin_sine_open_loop.rs` to the previously validated observer-only controller:
    - kept the observer and its telemetry
    - reflashed before stopping so the board would not remain on a known-regressed assist build
57. Re-checking the restored observer-only image did not reproduce the earlier `220 Hz` success in the final three bench passes:
    - one rerun aborted around `61.31 Hz`
    - one rerun aborted around `112.87 Hz`
    - this indicates the bench behavior had become inconsistent late in the session even after the source was restored
    - stopped there and left the supply output off rather than pushing further on an unstable setup
58. Started the explicit long-run experiment to get a full `10 s` spin window before adding a real control input:
    - changed the profile in `spin_sine_open_loop.rs` to:
      - ramp from `4.00 Hz` to `170.00 Hz` electrical
      - hold for `28_000` control updates
      - cap commanded amplitude at `10%`
    - total active spin window remained `40_000` updates at `4 kHz` (`10 s`)
59. First long-run validation at `12.0 V / 1.50 A`:
    - observer locked and the controller reached the full `170 Hz` hold
    - sustained the hold much longer than the earlier `1.25 A` attempts
    - still self-disarmed on bus sag at `idx=28256`
    - abort telemetry:
      - `bus_mv=10503`
      - `amp_pct=10`
      - `elec_hz_x100=17000`
    - this corresponds to about `7.06 s` into the active spin loop, so the remaining gap was current headroom, not startup stability
60. Increased the DPS current limit one more step, still within the initial safety ceiling:
    - kept voltage at `12.0 V`
    - raised current limit from `1.50 A` to `1.75 A`
    - left the firmware image otherwise unchanged for an apples-to-apples retry
61. Second long-run validation at `12.0 V / 1.75 A` completed successfully:
    - observer locked and stayed locked through the full run
    - reached `idx=39999` with no `bus_abort`
    - the controller completed the entire `10 s` active spin profile and returned cleanly to `disarmed`
    - no bench trip occurred; DPS protection remained `OK`
62. Returned the bench to a passive state after the successful long run:
    - reflashed `rtt_smoke` and verified continuous RTT output (`rtt_smoke_start`, then `rtt_smoke_tick ...`)
    - restored DPS current limit to `1.25 A`
    - left the DPS configured for `12.0 V / 1.25 A`, output `OFF`, protection `OK`

## Safety Status

- The currently flashed image is `boards/DM0001/bringup/firmware/src/bin/rtt_smoke.rs`.
- Current bench setup is configured for `12.0 V / 1.25 A`, output `OFF`, protection `OK`.
- The most recent validated motor-drive profile is the droop-aware observer-assisted sine drive with:
  - `170 Hz` electrical target
  - `10%` amplitude ceiling
  - `10 s` total active spin window
  - automatic bus backoff and bus-abort protection
  - sensorless phase-trim feedback with observer telemetry
- RTT logging remains confirmed and was used for all motor-drive runs.
- The board has been returned to a passive RTT smoke image, so the next power-up will not drive the motor.
- The best validated long-duration run in this log is now the full `10 s` profile at `12.0 V / 1.75 A`.
