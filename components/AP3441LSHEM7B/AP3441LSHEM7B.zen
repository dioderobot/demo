"""AP3441LSHE-7B Component"""

# Pins
Pins = struct(
    EN = io("EN", Net),
    EP = io("EP", Net),
    FB = io("FB", Net),
    LX = io("LX", Net),
    NC = io("NC", Net),
    PG = io("PG", Net),
    PGND = io("PGND", Net),
    SGND = io("SGND", Net),
    VIN = io("VIN", Net),
)

# Component
Component(
    name = "AP3441LSHE-7B",
    symbol = Symbol(library = "AP3441LSHE-7B.kicad_sym"),
    footprint = File("AP3441LSHE7B.kicad_mod"),
    pins = {
        "EN": Pins.EN,
        "EP": Pins.EP,
        "FB": Pins.FB,
        "LX": Pins.LX,
        "NC": Pins.NC,
        "PG": Pins.PG,
        "PGND": Pins.PGND,
        "SGND": Pins.SGND,
        "VIN": Pins.VIN,
    },
    properties = {
        "value": "AP3441LSHE-7B",
        "mpn": "AP3441LSHE-7B",
    },
)

add_property("layout_path", "build/preview")
