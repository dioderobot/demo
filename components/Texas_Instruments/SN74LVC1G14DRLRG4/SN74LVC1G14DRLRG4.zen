"""
SN74LVC1G14DRLRG4

Auto-generated using `pcb search`. Do not modify.
"""

mpn = config("mpn", str, optional=True, default="SN74LVC1G14DRLRG4")

Pins = struct(
    A = io("A", Net),
    GND = io("GND", Net),
    N_C = io("N_C", Net),
    VCC = io("VCC", Net),
    Y = io("Y", Net)
)

Component(
    name = "SN74LVC1G14DRLRG4",
    mpn = mpn,
    manufacturer = "Texas Instruments",
    datasheet = File("SN74LVC1G14DRLRG4.pdf"),
    symbol = Symbol(library = "SN74LVC1G14DRLRG4.kicad_sym"),
    footprint = File("SOTFL50P160X60-5N.kicad_mod"),
    pins = {
        "A": Pins.A,
        "GND": Pins.GND,
        "N.C.": Pins.N_C,
        "VCC": Pins.VCC,
        "Y": Pins.Y
    },
)