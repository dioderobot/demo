# ============================================================================
# TCAN330DCNT CAN Transceiver Reference Design
# Author: Nasheed Ur Rehman
# Datasheet: <link>
# ============================================================================

# ----------------------------------------------------------------------------
# Libraries & Dependencies
# ----------------------------------------------------------------------------

load("@stdlib/units.zen", "Voltage", "Current", "Capacitance", "Inductance", "Frequency", "Resistance")

# ----------------------------------------------------------------------------
# Component Modules
# ----------------------------------------------------------------------------

TCAN330DCNT = Module("github.com/dioderobot/demo/components/Texas_Instruments/TCAN330DCNT/TCAN330DCNT.zen")
Capacitor = Module("@stdlib/generics/Capacitor.zen")

# ----------------------------------------------------------------------------
# Configuration Parameters
# ----------------------------------------------------------------------------

# Capacitor voltage ratings
decoupling_cap_voltage = config(str, default="16V")

# Capacitor values
decoupling_cap_value = config(str, default="100nF")

# ----------------------------------------------------------------------------
# Global Power Interfaces
# ----------------------------------------------------------------------------

VCC = io(Power)
GND = io(Ground)

# ----------------------------------------------------------------------------
# Global IO Nets
# ----------------------------------------------------------------------------

CAN_RX = io(Net)
CAN_TX = io(Net)
CAN_H = io(Net)
CAN_L = io(Net)
SHDN = io(Net)

# ============================================================================
# Components
# ============================================================================

TCAN330DCNT(
    name="TCAN330DCNT",
    VCC=VCC,
    GND=GND,
    CANH=CAN_H,
    CANL=CAN_L,
    SHDN=SHDN,
    RXD=CAN_RX,
    TXD=CAN_TX,
    S=GND,
)

Capacitor(
    name="C_VCC",
    value=decoupling_cap_value,
    voltage=decoupling_cap_voltage,
    package="0402",
    dielectric="X7R",
    P1=VCC,
    P2=GND,
)

# ============================================================================
# PCB Schematic
# ============================================================================


# pcb:sch C_VCC.C x=385.0800 y=87.9000 rot=0
# pcb:sch TCAN330DCNT.TCAN330DCNT x=634.0000 y=-1.0000 rot=0
# pcb:sch GND.1 x=773.7000 y=202.2000 rot=0
# pcb:sch VCC.1 x=397.7800 y=37.1000 rot=0
